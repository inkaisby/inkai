"use client";

/**
 * Entry point modul Settings.
 * Re-export SettingsView agar import dari module konsisten (page bisa import dari sini atau dari components/SettingsView).
 */
export { default } from "./components/SettingsView";
